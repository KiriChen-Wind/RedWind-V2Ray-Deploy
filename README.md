# RedWind-V2Ray-Deploy
RedWind Team V2Ray，Socks、VMess一键部署脚本

## 功能特性

每次部署时会自动创建：
- **VMess-TCP** 代理服务（随机端口）
- **Socks** 代理服务（随机端口）
  - 用户名：`RedWind`
  - 密码：`RedWind+端口号`（例如端口为12345，则密码为RedWind12345）

部署完成后会在屏幕上显示两个服务的完整配置信息。
